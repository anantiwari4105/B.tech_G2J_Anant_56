#include<stdio.h>
int main()
{
    int a,b,c;
    printf("enter side a");
    scanf("%d",&a);
    printf("enter side b");
    scanf("%d",&b);
    printf("enter side c");
    scanf("%d",&c);
    
    if(a+b>c && a+c>b && b+c>a)
    {
        printf("the triangle exists");
        
    }
    
    else 
    {
        printf("the triangle do not exists");
    }
    
    if (a==b && b==c)
    {
        printf("the triangle is eqilateral triangle");
    }
    
    else if (a==b || b==c || c==a)
    
    {
        printf("the triangle is isoscales traingle");
    }
    
    else
    {
        printf("the triangle is scales triangle");
    }
    
    return 0;
}